#pragma once

namespace Yuki {

	/*class ErrorReporter
	{
	public:
		virtual void ProcessNext() = 0;

		virtual void Report() = 0;
		virtual bool HasError() const = 0;
	};

	class DefaultErrorReporter : public ErrorReporter
	{
	public:
		void ProcessNext() override {}

		void Report() override { m_ErrorCount++; }
		bool HasError() const override { return m_ErrorCount > 0; }

	private:
		uint16_t m_ErrorCount = 0;
	};*/

}
